# ChartDB, for viewing datamodel/snowtime.chartdb.json. Start with `bun run datamodel`.
name: snowtime-datamodel
services:
  chartdb:
    image: ghcr.io/chartdb/chartdb:latest
    ports:
      - '8080:80'
    environment:
      DISABLE_ANALYTICS: 'true'
